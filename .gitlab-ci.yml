before_script:
  - sudo apt-get update
deploy:
  script:
  - echo 'Starting Tests'
  - sudo yarn 
  - sudo yarn test
  - echo 'Starting deploy'
  - sudo git -C /root/alltitude pull
  - echo 'Build Nuxt'
  - sudo yarn --cwd /root/alltitude/ build
