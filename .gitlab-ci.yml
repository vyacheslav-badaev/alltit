before_script:
  - sudo apt-get update
  - sudo chown -R gitlab-runner:gitlab-runner .
deploy:
  script:
  - echo 'Starting Testss'
  - sudo rm -rf yarn.lock 
  - sudo yarn
  - sudo chown -R gitlab-runner:gitlab-runner .
  - sudo yarn test
  - echo 'Starting deploy'
  - sudo git -C /root/alltitude pull
  - echo 'Build Nuxt'
  - sudo yarn --cwd /root/alltitude/ build
